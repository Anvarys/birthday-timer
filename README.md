## Birthday timer

A simple front-end only website that allows you to create a birthday timer for your birthday and share it with others via a url, with which the website will generate a timer (React router)

### Development

To host this website locally you can run it via npm after cloning the repository

```bash
git clone https://github.com/Anvarys/birthday-timer.git
cd birthday-timer
npm run dev
```